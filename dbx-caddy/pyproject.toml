[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "dbx-caddy"
version = "0.0.1+rev1cc740c"
requires-python = ">=3.10.12,<3.13"
dependencies = ["PyGithub"]
