[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "dbx-caddy"
version = "0.0.1+rev0ec6938"
requires-python = ">=3.11,<3.13"
dependencies = ["PyGithub"]
