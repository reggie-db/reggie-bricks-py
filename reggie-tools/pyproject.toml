[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "reggie-tools"
dependencies = [
    "reggie-core @ file://${PROJECT_ROOT}/../reggie-core",
    "databricks-sdk",
    "databricks-connect<17.0",
    "requests",
    "urllib3",
    "pydantic",
]
version = "0.0.1+g790b502"
requires-python = ">=3.12"

[tool.uv.sources.reggie-core]
workspace = true
