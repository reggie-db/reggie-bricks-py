[build-system]
requires = [
    "uv_build>=0.9.6,<0.10.0",
]
build-backend = "uv_build"

[project]
name = "reggie-tools"
version = "0.0.1+g62ad301"
dependencies = [
    "reggie-core @ file://${PROJECT_ROOT}/../reggie-core",
    "databricks-sdk",
    "databricks-connect<=17.1",
    "requests",
    "urllib3",
    "pydantic",
]

[tool.uv.sources.reggie-core]
workspace = true
