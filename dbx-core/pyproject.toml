[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "dbx-core"
version = "0.0.1+rev3f020a3"
requires-python = ">=3.11,<3.13"
dependencies = ["lfp-logging"]

[dependency-groups]
dev = ["pydantic>=2.12.5"]
