[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "dbx-ip"
version = "0.0.1+rev183df93"
requires-python = ">=3.11,<3.13"
dependencies = ["dbx-core @ file://${PROJECT_ROOT}/../dbx-core"]

[tool.uv.sources.dbx-core]
workspace = true
