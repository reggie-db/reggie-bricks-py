[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "reggie-reflex"
version = "0.0.1+g0f12611"
requires-python = ">=3.12,<3.13"
dependencies = [
    "reggie-core @ file://${PROJECT_ROOT}/../reggie-core",
    "reflex"
]

[tool.uv.sources.reggie-core]
workspace = true
