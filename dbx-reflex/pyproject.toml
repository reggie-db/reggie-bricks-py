[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "dbx-reflex"
version = "0.0.1+rev010fbbe"
requires-python = ">=3.11,<3.13"
dependencies = ["reflex>=0.8.26"]

[tool.uv_build]
package-dir = "."
packages = [{ include = "dbx_reflex" }]
