[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "build"
version = "0.0.1+gf0b5471"
requires-python = ">=3.12,<3.13"

[dependency-groups]
dev = [
  "reggie-build @ git+https://github.com/reggie-db/reggie-build-py.git",
  "requests"
]
